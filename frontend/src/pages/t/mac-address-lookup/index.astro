---
import type { Tool } from '@/config/tools';
import { getToolByKey } from '@/config/tools';
import ToolDetailLayout from '@/components/tool/ToolDetailLayout.astro';
import MacAddressLookup from './_MacAddressLookup';

const tool = getToolByKey('mac-address-lookup') as Tool;
---

<ToolDetailLayout tool={tool}>
  <MacAddressLookup client:load />
</ToolDetailLayout>
