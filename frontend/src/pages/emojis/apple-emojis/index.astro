---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import AdBanner from '../../../components/banner/AdBanner';
import { getAllEmojis, getEmojiCategories } from '../../../lib/emojis';

const emojis = await getAllEmojis();

// Same logic for grouping emojis by category
const categories = getEmojiCategories();
const emojisByCategory: Record<string, any[]> = {};
for (const cat of categories) {
  emojisByCategory[cat] = emojis.filter(
    (e) =>
      (e.fluentui_metadata?.group ||
        e.emoji_net_data?.category ||
        (e as any).given_category ||
        'Other') === cat
  );
}

// Sort categories and emojis
const sortedCategories = Object.keys(emojisByCategory).sort();
for (const category of sortedCategories) {
  emojisByCategory[category].sort((a, b) => {
    const titleA = a.title || a.fluentui_metadata?.cldr || a.slug || '';
    const titleB = b.title || b.fluentui_metadata?.cldr || b.slug || '';
    return titleA.localeCompare(titleB);
  });
}

// Icons for categories
const categoryIconMap: Record<string, string> = {
  "Smileys & Emotion": "😀",
  "People & Body": "👤",
  "Animals & Nature": "🐶",
  "Food & Drink": "🍎",
  "Travel & Places": "✈️",
  "Activities": "⚽",
  "Objects": "📱",
  "Symbols": "❤️",
  "Flags": "🏁",
  "Other": "❓"
};
---

<BaseLayout 
  name="Apple Emojis"
  path="/emojis/apple-emojis/"
  title="Apple Emojis Reference - Browse & Copy Apple Emojis | Online Free DevTools by Hexmos"
  description="Browse Apple's version of emojis by category. Copy instantly, explore meanings, and discover platform-specific emoji designs."
  canonical="https://hexmos.com/freedevtools/emojis/apple-emojis/"
  themeColor="#1e40af"
  keywords={['apple emojis', 'ios emojis', 'apple emoji list', 'apple emoji reference']}
  showSidebar={false}
  showHeader={true}
>
  <div class="max-w-6xl mx-auto px-2 md:px-6">
    <div class="mb-16 mt-[74px]">
      <AdBanner />
    </div>
    
    <div class="mb-8">
      <nav class="text-sm text-slate-600 dark:text-slate-400 mb-4">
        <a href="/freedevtools/" class="hover:text-blue-600 dark:hover:text-blue-400">Free DevTools</a>
        <span class="mx-2">/</span>
        <a href="/freedevtools/emojis/" class="hover:text-blue-600 dark:hover:text-blue-400">Emojis</a>
        <span class="mx-2">/</span>
        <span>Apple Emojis</span>
      </nav>
      
      <h1 class="text-3xl font-bold text-slate-900 dark:text-slate-100 mb-4">
        Apple Emojis 🍏
      </h1>
      <p class="text-lg text-slate-600 dark:text-slate-400 mb-6">
        Explore Apple-style emojis across all categories. Perfect for previewing iOS emoji appearances and copying instantly.
      </p>
    </div>

    <div id="emoji-categories" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
      {sortedCategories.map((category) => (
        <div class="category-card bg-white dark:bg-slate-900 border border-slate-200 dark:border-slate-900 rounded-xl p-4 transition-all duration-300 ease-in-out shadow-sm hover:-translate-y-1 hover:shadow-xl">
          <div class="flex items-center mb-4">
            <div class="w-10 h-10 bg-gradient-to-br from-green-500 to-green-600 dark:from-green-600 dark:to-green-700 rounded-lg flex items-center justify-center mr-3 border border-slate-200 dark:border-slate-700 shadow-sm">
              <span class="text-2xl">{categoryIconMap[category] || "❓"}</span>
            </div>
            <a 
              href={`/freedevtools/emojis/apple-emojis/${category.toLowerCase().replace(/[^a-z0-9]+/g, '-')}`}
              class="text-xl font-semibold text-black dark:text-neon-light capitalize hover:text-green-600 dark:hover:text-green-400 transition-colors"
            >
              {category}
            </a>
          </div>
          
          <p class="text-sm text-slate-800 dark:text-slate-400 mb-4">
            {emojisByCategory[category].length} emojis available
          </p>
        </div>
      ))}
    </div>

    <!-- Navigation -->
    <div class="mt-10 pt-6 mb-8 border-t border-slate-200 dark:border-slate-700">
      <div class="flex flex-wrap gap-4">
        <a 
          href="/freedevtools/emojis/" 
          class="inline-flex items-center px-4 py-2 text-sm font-medium text-blue-600 dark:text-blue-400 bg-blue-50 dark:bg-blue-900/20 rounded-lg hover:bg-blue-100 transition-colors"
        >
          ←  Back to Emojis
        </a>
      </div>
    </div>
  </div>
</BaseLayout>
