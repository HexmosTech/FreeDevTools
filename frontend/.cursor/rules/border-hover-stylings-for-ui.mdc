---
alwaysApply: true
---
Always use `border-fdt-yellow-dark` for light mode and `border-fdt-yellow` for dark mode when applying hover or focus border colors.

**Colors:**
- Light mode: `border-fdt-yellow-dark` (#b6b000)
- Dark mode: `border-fdt-yellow` (#d4cb24)

Apply this consistently across all search inputs, buttons, and interactive elements.
