package t

import (
	"fdt-templ/components/common"
	"fdt-templ/components/layouts"
		"fdt-templ/internal/db/tools"
)

templ EnvToNetlifyToml() {
	@layouts.BaseLayout(layouts.BaseLayoutProps{
		Title:        tools.ToolsConfig["env-to-netlify-toml"].Title,
		Description:  tools.ToolsConfig["env-to-netlify-toml"].Description,
		Canonical:    tools.ToolsConfig["env-to-netlify-toml"].Canonical,
		Keywords:     tools.ToolsConfig["env-to-netlify-toml"].Keywords,
		OgImage:      tools.ToolsConfig["env-to-netlify-toml"].OgImage,
		TwitterImage: tools.ToolsConfig["env-to-netlify-toml"].TwitterImage,
		ShowHeader:   true,
		
	}) {
		<div id="env-to-netlify-toml-root"></div>
		<script type="module">
			import '/freedevtools/static/js/index.js';
			renderTool('env-to-netlify-toml', 'env-to-netlify-toml-root');
		</script>
		<div class="max-w-6xl mx-auto px-2 md:px-6 mb-10 mt-12">
			@common.SeeAlso()
		</div>
	}
}
