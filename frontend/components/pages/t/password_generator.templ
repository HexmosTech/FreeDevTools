package t

import (
	"fdt-templ/components/common"
	"fdt-templ/components/layouts"
		"fdt-templ/internal/db/tools"
)

templ PasswordGenerator() {
	@layouts.BaseLayout(layouts.BaseLayoutProps{
		Title:        tools.ToolsConfig["password-generator"].Title,
		Description:  tools.ToolsConfig["password-generator"].Description,
		Canonical:    tools.ToolsConfig["password-generator"].Canonical,
		Keywords:     tools.ToolsConfig["password-generator"].Keywords,
		OgImage:      tools.ToolsConfig["password-generator"].OgImage,
		TwitterImage: tools.ToolsConfig["password-generator"].TwitterImage,
		ShowHeader:   true,
		
	}) {
		<div id="password-generator-root"></div>
		<script type="module">
			import '/freedevtools/static/js/index.js';
			renderTool('password-generator', 'password-generator-root');
		</script>
		<div class="max-w-6xl mx-auto px-2 md:px-6 mb-10 mt-12">
			@common.SeeAlso()
		</div>
	}
}
