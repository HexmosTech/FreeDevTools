package t

import (
	"fdt-templ/components/common"
	"fdt-templ/components/layouts"
		"fdt-templ/internal/db/tools"
)

templ HarFileViewer() {
	@layouts.BaseLayout(layouts.BaseLayoutProps{
		Title:        tools.ToolsConfig["har-file-viewer"].Title,
		Description:  tools.ToolsConfig["har-file-viewer"].Description,
		Canonical:    tools.ToolsConfig["har-file-viewer"].Canonical,
		Keywords:     tools.ToolsConfig["har-file-viewer"].Keywords,
		OgImage:      tools.ToolsConfig["har-file-viewer"].OgImage,
		TwitterImage: tools.ToolsConfig["har-file-viewer"].TwitterImage,
		ShowHeader:   true,
		
	}) {
		<div id="har-file-viewer-root"></div>
		<script type="module">
			import '/freedevtools/static/js/index.js';
			renderTool('har-file-viewer', 'har-file-viewer-root');
		</script>
		<div class="max-w-6xl px-2 md:px-6 mb-10 mt-12">
			@common.SeeAlso()
		</div>
	}
}
