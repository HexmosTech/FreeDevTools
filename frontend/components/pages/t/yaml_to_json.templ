package t

import (
	"fdt-templ/components/common"
	"fdt-templ/components/layouts"
		"fdt-templ/internal/db/tools"
)

templ YamlToJson() {
	@layouts.BaseLayout(layouts.BaseLayoutProps{
		Title:        tools.ToolsConfig["yaml-to-json"].Title,
		Description:  tools.ToolsConfig["yaml-to-json"].Description,
		Canonical:    tools.ToolsConfig["yaml-to-json"].Canonical,
		Keywords:     tools.ToolsConfig["yaml-to-json"].Keywords,
		OgImage:      tools.ToolsConfig["yaml-to-json"].OgImage,
		TwitterImage: tools.ToolsConfig["yaml-to-json"].TwitterImage,
		ShowHeader:   true,
		
	}) {
		<div id="yaml-to-json-root"></div>
		<script type="module">
			import '/freedevtools/static/js/index.js';
			renderTool('yaml-to-json', 'yaml-to-json-root');
		</script>
		<div class="max-w-6xl px-2 md:px-6 mb-10 mt-12">
			@common.SeeAlso()
		</div>
	}
}
