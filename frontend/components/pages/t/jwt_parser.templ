package t

import (
	"fdt-templ/components/common"
	"fdt-templ/components/layouts"
		"fdt-templ/internal/db/tools"
)

templ JwtParser() {
	@layouts.BaseLayout(layouts.BaseLayoutProps{
		Title:        tools.ToolsConfig["jwt-parser"].Title,
		Description:  tools.ToolsConfig["jwt-parser"].Description,
		Canonical:    tools.ToolsConfig["jwt-parser"].Canonical,
		Keywords:     tools.ToolsConfig["jwt-parser"].Keywords,
		OgImage:      tools.ToolsConfig["jwt-parser"].OgImage,
		TwitterImage: tools.ToolsConfig["jwt-parser"].TwitterImage,
		ShowHeader:   true,
		
	}) {
		<div id="jwt-parser-root"></div>
		<script type="module">
			import '/freedevtools/static/js/index.js';
			renderTool('jwt-parser', 'jwt-parser-root');
		</script>
		<div class="max-w-6xl px-2 md:px-6 mb-10 mt-12">
			@common.SeeAlso()
		</div>
	}
}
